from setuptools import find_packages, setup

# Configure the setup for the 'rag' package
setup(
    name="rag",  # The name of the package
    version="0.1",  # The version of the package
    packages=find_packages(),  # Automatically find and include all sub-packages
)
